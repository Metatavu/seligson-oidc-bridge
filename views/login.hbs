<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="/css/login-form.css" rel="stylesheet" />
    <title>{{ strings.name }}</title>
  </head>
  <body>
    <div class="login-form">
      <div class="logo-container">
        <img class="logo" src="/logo.svg"/>
      </div>
      <div class="header-container">
        <h1>{{ strings.name }}</h1>
      </div>      
      <form autocomplete="off" action="/oidc/interaction/{{ uid }}" method="post">
        {{#if error}}
        <div class="error-container"><p>{{ error }}</p></div>
        {{/if}}
        <div class="form-field-container username-container">
          <input type="text" name="login" placeholder="{{ strings.username }}" required {{#unless params.login_hint }}autofocus="on"{{else}} value="{{ params.login_hint }}" {{/unless}}>
        </div>
        <div class="form-field-container password-container">
          <input type="password" name="password" placeholder="{{ strings.password }}" required  {{#if params.login_hint }}autofocus="on"{{/if}}>
        </div>
        <div class="form-field-container submit-container">
          <input type="submit" value="{{ strings.login }}">
        </div>
      </form>   
      <footer>
        {{ strings.footerText }}
      </footer>   
    </div>
  </body>
</html>